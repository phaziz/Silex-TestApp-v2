<IfModule mod_rewrite.c>
    Options -MultiViews
    RewriteEngine On

    	#Running MAMP @localhost UPDATE THIS IF YOU NEED
    	RewriteBase /localmamp/silex/web/

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [QSA,L]
</IfModule>